'use client';import{useRouter,useSearchParams}from'next/navigation';export function DashboardPeriodFilter(){const r=useRouter(),s=useSearchParams(),c=s.get('period')??'month';function change(p:string){const q=new URLSearchParams(s.toString());q.set('period',p);r.push(`/?${q.toString()}`)}return <div className="period-filter">{[['today','Hoy'],['week','7 días'],['month','Mes'],['quarter','90 días']].map(([v,l])=><button key={v} className={`btn ${c===v?'':'secondary'}`} onClick={()=>change(v)}>{l}</button>)}</div>}