import {createClient} from "@/lib/supabase/server"; export default async function Page(){const s=await createClient();const{data,error}=await s.from("communication_log").select("id,recipient,message,status,created_at,customers(full_name),orders(order_number)").order("created_at",{ascending:false}).limit(200);if(error)return <div className="error">{error.message}</div>;return <><h1 className="page-title">Historial de comunicaciones</h1><section className="panel"><div className="table-wrap"><table><thead><tr><th>Fecha</th><th>Cliente</th><th>Pedido</th><th>Destino</th><th>Estado</th><th>Mensaje</th></tr></thead><tbody>{(data??[]).map((x:any)=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString("es-AR")}</td><td>{x.customers?.full_name??"—"}</td><td>{x.orders?.order_number?`#${x.orders.order_number}`:"—"}</td><td>{x.recipient??"—"}</td><td>{x.status}</td><td className="template-preview">{x.message}</td></tr>)}</tbody></table></div></section></>}
