import {NotificationCenter} from "@/components/notification-center"; import {createClient} from "@/lib/supabase/server"; export default async function Page(){const s=await createClient();const{data,error}=await s.from("notification_events").select("*").eq("is_read",false).order("created_at",{ascending:false});if(error)return <div className="error">{error.message}</div>;return <NotificationCenter initial={data??[]}/>}
