import {currency,products,settings} from "@/lib/data";
export default function Page(){return <><h1 className="page-title">Productos</h1><p className="page-subtitle">Precios y costos estimados.</p><section className="panel"><div className="table-wrap"><table><thead><tr><th>Producto</th><th>Categoría</th><th>Costo</th><th>Precio</th><th>Margen</th><th>Sugerido</th></tr></thead><tbody>{products.map(p=>{const m=(p.price-p.cost)/p.price*100;const s=p.cost/(1-settings.targetMargin/100);const tone=m>=50?'good':m>=35?'warn':'danger';return <tr key={p.name}><td>{p.name}</td><td>{p.category}</td><td>{currency(p.cost)}</td><td>{currency(p.price)}</td><td><span className={`badge ${tone}`}>{m.toFixed(1)}%</span></td><td>{currency(s)}</td></tr>})}</tbody></table></div></section></>}
