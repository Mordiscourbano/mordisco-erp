import{InventoryManager}from"@/components/inventory-manager";import{createClient}from"@/lib/supabase/server";export default async function Page(){const s=await createClient();const{data,error}=await s.from('inventory_levels').select('id,ingredient_id,current_quantity,minimum_quantity,target_quantity,ingredients(name,base_unit)').order('updated_at',{ascending:false});if(error)return <div className="error">{error.message}</div>;return <InventoryManager initial={(data||[]) as never[]}/>}