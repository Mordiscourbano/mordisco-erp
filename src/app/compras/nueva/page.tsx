import{PurchaseOrderManager}from'@/components/purchase-order-manager';import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const[{data:suppliers,error:e1},{data:ingredients,error:e2}]=await Promise.all([s.from('suppliers').select('id,name').eq('active',true).order('name'),s.from('ingredients').select('id,name,purchase_unit,purchase_price').order('name')]);if(e1)return <div className="error">{e1.message}</div>;if(e2)return <div className="error">{e2.message}</div>;return <PurchaseOrderManager suppliers={suppliers||[]} ingredients={ingredients||[]}/>}
