import{PurchaseReceiver}from'@/components/purchase-receiver';import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const{data,error}=await s.from('purchase_orders').select('id,order_number,status,total,suppliers(name),purchase_order_items(ingredient_id,ordered_quantity,received_quantity,unit_cost,ingredients(name,purchase_unit))').order('created_at',{ascending:false});if(error)return <div className="error">{error.message}</div>;return <PurchaseReceiver orders={(data||[]) as never[]}/>}
