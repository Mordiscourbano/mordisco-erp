import {PosManager} from '@/components/pos-manager';import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const{data,error}=await s.from('products').select('id,name,category,current_price').eq('active',true).order('category').order('name');if(error)return <div className="error">{error.message}</div>;return <PosManager products={data||[]}/>;}