import {currency,products,settings} from "@/lib/data";
export default function Page(){const fixed=settings.rent+settings.electricity+settings.internet+settings.salaries+settings.taxes+settings.accountant+settings.other;const ticket=settings.monthlyRevenue/settings.orders;const ranked=[...products].map(p=>({...p,contribution:(p.price-p.cost)*p.sold})).sort((a,b)=>b.contribution-a.contribution).slice(0,8);return <><h1 className="page-title">Resumen del negocio</h1><p className="page-subtitle">Datos iniciales cargados para validar el modelo.</p><section className="grid kpis"><article className="card"><span>Facturación</span><strong>{currency(settings.monthlyRevenue)}</strong></article><article className="card"><span>Comandas</span><strong>{settings.orders.toLocaleString("es-AR")}</strong></article><article className="card"><span>Ticket promedio</span><strong>{currency(ticket)}</strong></article><article className="card"><span>Costos fijos</span><strong>{currency(fixed)}</strong></article></section><section className="panel"><h2>Aporte estimado por producto</h2><div className="table-wrap"><table><thead><tr><th>Producto</th><th>Categoría</th><th>Vendidos</th><th>Aporte</th></tr></thead><tbody>{ranked.map(p=><tr key={p.name}><td>{p.name}</td><td>{p.category}</td><td>{p.sold}</td><td>{currency(p.contribution)}</td></tr>)}</tbody></table></div></section><section className="panel"><div className="notice">Los costos por producto son estimaciones provisionales. Sprint 2 los calculará desde recetas, aceite y packaging.</div></section></>}
