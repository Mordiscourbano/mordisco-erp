import{SuppliersManager}from'@/components/suppliers-manager';import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const{data:p}=await s.from('profiles').select('business_id').single();const{data,error}=await s.from('suppliers').select('*').order('name');if(error)return <div className="error">{error.message}</div>;return <SuppliersManager initial={data||[]} businessId={p!.business_id}/>}
